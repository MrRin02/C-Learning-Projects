#include <stdio.h>

//A program that outputs values of type char as a character and as an integer

int main(){
	
	char ch = 'T';
	char sym = '?';
	
	printf("\nThe first example as a letter looks like this - %c", ch);
	printf("\nThe first example as a letter looks like this - %d", ch);
	printf("\nThe first example as a letter looks like this - %c", sym);
	printf("\nThe first example as a letter looks like this - %d", sym);
	return 0;
	
	
}
